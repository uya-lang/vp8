use libc.malloc;

fn bytes_for_i16() usize {
    return @size_of(i16);
}

fn main() i32 {
    const ptr: &byte = malloc(bytes_for_i16()) as &byte;
    if ptr == null {
        return 1;
    }
    return 0;
}
